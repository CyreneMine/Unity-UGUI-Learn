# 综合 UI 案例复盘

本阶段从第 46 节开始，目标是完成登录、注册、服务器和选服相关面板，并逐步串联 UI 管理、JSON 数据和面板跳转。

## 当前进度

- 已完成提示面板 Prefab 与逻辑。
- 已完成登录面板拼接。
- 已按教程完成登录面板当前章节逻辑。
- 已完成注册面板拼接和注册逻辑。
- 服务器面板尚未完成，因此登录成功后进入服务器面板、自动登录跳转按课程安排暂时保留 TODO。

## 提示面板

- `TipPanel` 通过 `UIManager` 动态创建。
- 支持动态修改提示文本。
- 确认按钮关闭提示面板。
- 调试时发现：UI 最终显示位置不等于射线检测范围，文本对象的 RectTransform 如果覆盖按钮，仍会挡住按钮点击。

## 登录面板

- 已制作 `Resources/UI/LoginPanel` Prefab。
- 面板包含账号输入、密码输入、记住密码、自动登录、登录按钮和注册按钮。
- 已绑定 `LoginPanel` 脚本引用。
- 当前逻辑已读取 `LoginData`，并根据记住密码和自动登录状态恢复输入框与 Toggle。

## 待补全

- 登录按钮校验账号密码并进入服务器面板。
- 自动登录时直接进入服务器面板。
- 在服务器面板完成后，回到登录面板补齐跳转逻辑。

## 注册面板

- 已制作 `Resources/UI/RegisterPanel` Prefab。
- 面板包含账号输入、密码输入、确认按钮和取消按钮。
- 注册成功后写入 `RegisterData`，回到登录面板并回填账号与密码。
- 注册取消时返回登录面板。
